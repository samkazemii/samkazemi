# V16 — Persian Voice Recognition Fix

The browser speech-recognition language for Voice AI is now fixed to Iranian Persian (`fa-IR`), regardless of the website interface language.

Upload all files to GitHub Pages, replacing the existing files. Then hard-refresh the website.
